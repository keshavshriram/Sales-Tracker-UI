import * as configjson from '../assets/config.json';
import * as configUrlJson from '../assets/configUrl.json';
import * as configApiEndPoints from '../assets/configApiEndPoints.json';

export const environment = {
    production: false,
    configJson: configjson,
    configUrlJson : configUrlJson,
    configApiEndPoints: configApiEndPoints
}